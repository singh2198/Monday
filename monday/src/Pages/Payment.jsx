import React from 'react';
import { contextPrice } from './Pricing';
// import { useContext} from 'react';

const Payment = () => {
    // const {basic,pro,standard}= useContext(contextPrice);
    // console.log(basic,pro,standard)
    return (
        <div>
            
        </div>
    );
}

export default Payment;
